<p id="notice"><%= notice %></p>

<p>
  <strong>Desc:</strong>
  <%= @idea.desc %>
</p>

<%= link_to 'Edit', edit_idea_path(@idea) %> |
<%= link_to 'Back', ideas_path %>


<!DOCTYPE html>
<html>
<head>
  <meta charset=utf-8 />
  <title>A simple map</title>
  <meta name='viewport' content='initial-scale=1,maximum-scale=1,user-scalable=no' />
  <script src='https://api.mapbox.com/mapbox.js/v2.2.1/mapbox.js'></script>
  <link href='https://api.mapbox.com/mapbox.js/v2.2.1/mapbox.css' rel='stylesheet' />
  <style>
    body,html {height:100%;}

    #map { position:absolute; top:0; bottom:0; width:100%; }
  </style>
</head>
<body>


  <div class="row">
    <div class="col-sm-8">
      <div id='map'></div>

    </div>
    <div class="col-sm-4">kwjbflkdjsc</div>
  </div>
  767



  <script>
    var windowHeight = $(window).height();
    var windowWidth = $(window).width();
    if(windowWidth>767) {
      $('#map').css('height', windowHeight-55);
    }
    else {
      $('#map').css('height', windowHeight/2);
    }
    $( window ).resize(function(){
      var windowHeight = $(window).height();
      var windowWidth = $(window).width();
      if(windowWidth>767) {
        $('#map').css('height', windowHeight-55);
      }
      else {
        $('#map').css('height', windowHeight/3);
      }
    });


    L.mapbox.accessToken = 'pk.eyJ1IjoiYXN0YW1wZXIiLCJhIjoiVEhDY2ItQSJ9.-_A_O1pHHwyu-HJ3T3Z6CA';
    var map = L.mapbox.map('map', 'mapbox.streets')
    .setView([40, -74.50], 9);
  </script>


</body>
</html>
